/**
 * Assessment: assignments, submissions, and the projection into Scholar.
 *
 * The projection is the seam between institutional and personal: an assignment
 * owns the title, instructions and deadline; the task it produces owns the
 * estimate, priority, scheduling and logged time. See ./projection.ts.
 */
export * from "./projection";
